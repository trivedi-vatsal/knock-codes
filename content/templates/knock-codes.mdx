---
title: "Knock Codes"
description: "A segmented dark card — full-page backdrop, eight-box code entry, support link, and footer help text, all in one file."
category: "Templates"
tags: ["template", "segmented"]
registryName: "knock-codes-template"
version: "1.0.0"
relatedBlocks: ["knock-codes", "pin-input", "standalone-gate"]
useCase: "Client preview"
complexity: "Standard"
mode: "both"
bestUsedFor: "A polished, on-brand restricted-access page you can ship as-is, no assembly required."
accessibility: "Each code box has its own aria-label (\"Access code character N of 8\"), the group has role=\"group\", and Backspace/Arrow-key navigation between boxes works without a mouse. Errors and the submitting state announce through a shared aria-live=\"polite\" region, same contract as every other block."
customization: "Everything is one file — background, card, heading, description, code entry, button, support link, and footer are all inline, not composed from Gate Wrapper/PIN Input. Pass `logo` for your own wordmark, `codeLength`/`groupSize` to change the code shape, and `labels` to override every string. For a single masked field instead of segmented boxes, use Knock Codes + PIN Input instead."
props:
  - name: "expectedHash"
    type: "string"
    description: "SHA-256 hex hash to verify against, for local mode."
  - name: "verify"
    type: "VerifyFn"
    description: "Custom async verification function, for server mode."
  - name: "children"
    type: "ReactNode"
    required: true
    description: "Rendered once unlocked."
  - name: "logo"
    type: "ReactNode"
    description: "Rendered above the heading — your own logo/wordmark. Omitted entirely if not passed."
  - name: "codeLength"
    type: "number"
    default: "8"
    description: "Total number of code boxes."
  - name: "groupSize"
    type: "number"
    default: "4"
    description: "Boxes per group — a dash separates groups."
  - name: "supportHref"
    type: "string"
    description: "Renders \"Contact Support\" as a link to this URL."
  - name: "onContactSupport"
    type: "() => void"
    description: "Renders \"Contact Support\" as a button instead of a link — e.g. to open a chat widget."
  - name: "labels"
    type: "KnockCodesTemplateLabels"
    description: "Overrides heading, description, access-code label, support label, footer text, and every KnockCodesLabels string."
  - name: "fullPage"
    type: "boolean"
    default: "true"
    description: "Set false to embed this somewhere other than a real page root (a demo, a docs preview) — drops the full-viewport (100dvh) sizing."
  - name: "theme"
    type: '"light" | "dark"'
    description: "Forces light or dark presentation on its own, independent of any ancestor \".dark\" class. Omit to follow the nearest \".dark\" ancestor if one happens to exist."
  - name: "remember"
    type: '"session"'
    description: "Persists the unlocked session across reloads within the same tab via sessionStorage. Not a security boundary — see the security model."
  - name: "className"
    type: "string"
    description: "Extra classes on the full-page backdrop."
---

The fastest way to a fully-branded, polished restricted-access page — copy one file, wire a hash and a logo,
done. Paste a code across any box and it distributes automatically across the group.
