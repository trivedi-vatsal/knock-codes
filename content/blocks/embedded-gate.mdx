---
title: "Embedded Gate"
description: "A copy-paste React access screen for gating a widget or section inside an existing layout — Knock Codes, fixed to the inline shell. Zero dependencies."
category: "Routing & Layout"
tier: "alias"
tags: ["layout"]
registryName: "embedded-gate"
relatedBlocks: ["knock-codes", "standalone-gate", "protected-card"]
accessibility: "Identical to Knock Codes — only the outer positioning changes, not the semantics or keyboard flow."
customization: "Same full prop surface as Knock Codes, minus `variant` (fixed to \"inline\"). Use it for a sidebar widget or a gated section partway down a longer page, where a full-viewport takeover would be wrong."
bestUsedFor: "Gating one section of a longer page — a sidebar widget or a mid-page panel."
usageSnippet: |
  <EmbeddedGate expectedHash={process.env.NEXT_PUBLIC_KNOCK_HASH}>
    <SidebarWidget />
  </EmbeddedGate>
props:
  - name: "expectedHash"
    type: "string"
    description: "SHA-256 hex hash to verify against, for local mode."
  - name: "verify"
    type: "VerifyFn"
    description: "Custom async verification function, for server mode."
  - name: "storage"
    type: '"localStorage" | "sessionStorage" | "memory"'
    default: '"localStorage"'
    description: "Where the unlocked session persists."
  - name: "timeout"
    type: "number"
    default: "1800000"
    description: "Session lifetime in milliseconds."
  - name: "children"
    type: "ReactNode"
    required: true
    description: "Rendered once unlocked."
  - name: "labels"
    type: "KnockCodesLabels"
    description: "Overrides for every user-facing string."
---

The section-level counterpart to Standalone Gate's whole-app usage — same component, contained footprint.
