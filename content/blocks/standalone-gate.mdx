---
title: "Standalone Gate"
description: "The fastest path to a working gate: wrap your app, pass a hash, done."
category: "Routing & Layout"
tier: "alias"
tags: ["quickstart"]
registryName: "standalone-gate"
relatedBlocks: ["knock-codes", "embedded-gate"]
accessibility: "Same access-code entry UI as Knock Codes — full keyboard and screen-reader support inherited, none of it re-implemented."
customization: "Deliberately a smaller prop surface than Knock Codes — just `expectedHash`/`verify`, `children`, and an optional `heading`. Reach for `<KnockCodes>` directly the moment you need storage/timeout/activity-tracking control."
bestUsedFor: "The fastest possible integration — wrapping an entire app with minimal configuration."
props:
  - name: "expectedHash"
    type: "string"
    description: "SHA-256 hex hash to verify against, for local mode."
  - name: "verify"
    type: "VerifyFn"
    description: "Custom async verification function, for server mode."
  - name: "children"
    type: "ReactNode"
    required: true
    description: "Rendered once unlocked."
  - name: "heading"
    type: "string"
    default: '"This page is protected"'
    description: "Overrides just the heading — for everything else, use Knock Codes directly."
  - name: "variant"
    type: '"page" | "inline"'
    default: '"page"'
    description: "Escape hatch for embedding a demo somewhere other than a real app root — a docs page, this gallery's own preview."
---

For the two-line integration: generate a hash, wrap your app, ship it. No config to reason about.
