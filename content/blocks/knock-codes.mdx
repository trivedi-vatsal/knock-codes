---
title: "Knock Codes"
description: "A copy-paste React access screen for gating a whole page or app root — local hash or server verification, one prop swap between them. Zero dependencies."
category: "Core"
tags: ["core", "gate"]
registryName: "knock-codes"
relatedBlocks: ["protected-route", "standalone-gate", "protected-card"]
accessibility: "The access-code field is a labeled, masked text input with native paste support. Errors and the submitting state announce through an aria-live=\"polite\" status region, so screen reader users hear \"Checking...\" and any error without focus moving. The whole flow works keyboard-only: tab to the field, type, Enter to submit."
customization: "Every string is overridable through the `labels` prop for localization. The wrapper has no fixed visual opinions beyond the default card — pass `className` to restyle the shell, or skip this component and call `useKnockCodes` directly to build a fully custom prompt."
bestUsedFor: "The default choice for gating a whole page or app root in local mode."
usageSnippet: |
  <KnockCodes expectedHash={process.env.NEXT_PUBLIC_KNOCK_HASH}>
    <YourApp />
  </KnockCodes>
props:
  - name: "expectedHash"
    type: "string"
    description: "SHA-256 hex hash to verify against, for local mode. Mutually exclusive with verify."
  - name: "verify"
    type: "VerifyFn"
    description: "Custom async verification function, for server mode. Mutually exclusive with expectedHash."
  - name: "storage"
    type: '"localStorage" | "sessionStorage" | "memory"'
    default: '"localStorage"'
    description: "Where the unlocked session persists."
  - name: "storageKey"
    type: "string"
    default: '"knock-codes:session"'
    description: "Storage key the session is written under."
  - name: "timeout"
    type: "number"
    default: "1800000"
    description: "Session lifetime in milliseconds (30 minutes)."
  - name: "activityTracking"
    type: "boolean"
    default: "false"
    description: "Sliding-timeout model — interaction rewrites expiry instead of a fixed TTL."
  - name: "children"
    type: "ReactNode"
    required: true
    description: "Rendered once unlocked."
  - name: "labels"
    type: "KnockCodesLabels"
    description: "Overrides for every user-facing string."
  - name: "variant"
    type: '"page" | "inline"'
    default: '"page"'
    description: "Outer positioning — full-page centered, or flows inline in an existing layout."
  - name: "className"
    type: "string"
    description: "Extra classes on the outer wrapper."
---

The component every other block in this library either wraps or composes on. Local SHA-256 verification by
default; swap `expectedHash` for `verify` to upgrade to server-mode protection with no other code changes.

### Outer Positioning Variants

- **Full Page (`variant="page"`)**: Centers the gate vertically and horizontally across the viewport (`min-h-[100dvh]`). Replaces the thin `StandaloneGate` wrapper.
- **Inline Shell (`variant="inline"`)**: Flows naturally inside your existing layout (e.g. inside a sidebar, modal, or section card). Replaces the thin `EmbeddedGate` wrapper.
