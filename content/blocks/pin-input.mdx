---
title: "PIN Input"
description: "A copy-paste React access-code field for building a custom gate on useKnockCodes — masked field or segmented boxes, paste support, accessible errors. Zero dependencies."
category: "Core"
tags: ["core", "form"]
registryName: "pin-input"
relatedBlocks: ["knock-codes", "unlock-dialog", "verification-loader"]
accessibility: "Field variant: labeled via a real <label>, show/hide is a button with an aria-label. Boxes variant: role=group with per-slot aria-labels. Errors and the submitting label live in a shared aria-live=\"polite\" region."
customization: "`variant=\"field\"` (default) is a masked text input for arbitrary-length codes. `variant=\"boxes\"` is a fixed-length grid (`length`, `groupSize`). No verification logic of its own — drop it into any custom gate on `useKnockCodes`."
bestUsedFor: "Building a custom access-code form on useKnockCodes without adopting a full template's layout."
usageSnippet: |
  const { state, error, submit } = useKnockCodes({ expectedHash: hash });
  const [code, setCode] = useState("");

  <PinInput
    value={code}
    onChange={setCode}
    onSubmit={() => submit(code)}
    submitting={state === "submitting"}
    error={error}
  />
props:
  - name: "value"
    type: "string"
    required: true
    description: "Current input value (controlled)."
  - name: "onChange"
    type: "(value: string) => void"
    required: true
    description: "Called on every keystroke/paste."
  - name: "onSubmit"
    type: "() => void"
    required: true
    description: "Called on Enter or the submit button, when not empty and not submitting."
  - name: "submitting"
    type: "boolean"
    required: true
    description: "Disables the field and shows the busy label."
  - name: "error"
    type: "KnockCodesError | null"
    required: true
    description: "Drives which error message renders in the status region."
  - name: "labels"
    type: "KnockCodesLabels"
    description: "Overrides for every user-facing string."
  - name: "autoFocus"
    type: "boolean"
    description: "Focus the field (or first box) on mount."
  - name: "variant"
    type: '"field" | "boxes"'
    default: '"field"'
    description: "Masked text field, or a segmented character grid."
  - name: "length"
    type: "number"
    default: "6"
    description: "Slot count when variant is boxes."
  - name: "groupSize"
    type: "number"
    default: "4"
    description: "Dash between groups when variant is boxes."
---

The presentational primitive `<KnockCodes>` and every dialog-based block render internally. Independently
useful any time you're building a fully custom access-code form on top of the headless `useKnockCodes` hook.
