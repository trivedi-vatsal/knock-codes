---
title: "PIN Input"
description: "Masked access-code input (PIN or passphrase) with paste support and accessible loading/error states."
category: "Core"
tags: ["core", "form"]
registryName: "pin-input"
relatedBlocks: ["access-gate", "unlock-dialog", "verification-loader"]
accessibility: "Labeled via a real <label>, not just a placeholder. The show/hide toggle is a real button with an aria-label that flips with its state. Errors and the submitting label live in a shared aria-live=\"polite\" region."
customization: "Deliberately a single masked text field, not a segmented per-character box grid — that shape supports arbitrary-length passphrases, not just 4-6 digit PINs. It carries no verification logic of its own, so it drops into any custom gate UI built on `useAccessGate`."
bestUsedFor: "Building a custom access-code form on useAccessGate without adopting a full template's layout."
props:
  - name: "value"
    type: "string"
    required: true
    description: "Current input value (controlled)."
  - name: "onChange"
    type: "(value: string) => void"
    required: true
    description: "Called on every keystroke/paste."
  - name: "onSubmit"
    type: "() => void"
    required: true
    description: "Called on Enter or the submit button, when not empty and not submitting."
  - name: "submitting"
    type: "boolean"
    required: true
    description: "Disables the field and shows the busy label."
  - name: "error"
    type: "AccessGateError | null"
    required: true
    description: "Drives which error message renders in the status region."
  - name: "labels"
    type: "AccessGateLabels"
    description: "Overrides for every user-facing string."
  - name: "autoFocus"
    type: "boolean"
    description: "Focus the field on mount."
---

The presentational primitive `<AccessGate>` and every dialog-based block render internally. Independently
useful any time you're building a fully custom access-code form on top of the headless `useAccessGate` hook.
