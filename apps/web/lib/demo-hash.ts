/**
 * Precomputed sha256Hex("4242") (packages/core/hash.ts) — shared by every
 * live demo on the marketing site so none of them need an async hash
 * computation (and the loading state that implies) before first paint.
 */
export const DEMO_CODE = "4242";
export const DEMO_HASH = "0315b4020af3eccab7706679580ac87a710d82970733b8719e70af9b57e7b9e6";
