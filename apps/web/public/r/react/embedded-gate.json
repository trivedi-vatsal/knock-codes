{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "embedded-gate",
  "title": "Embedded Gate",
  "description": "Knock Codes fixed to the inline shell — sits naturally inside an existing layout (a sidebar widget, a section of a longer page) instead of taking over the full viewport.",
  "registryDependencies": [
    "https://knock.codes/r/react/knock-codes.json"
  ],
  "files": [
    {
      "path": "packages/react/EmbeddedGate.tsx",
      "content": "\"use client\";\r\n\r\nimport { KnockCodes, type KnockCodesProps } from \"./KnockCodes.tsx\";\r\n\r\nexport type EmbeddedGateProps = Omit<KnockCodesProps, \"variant\">;\r\n\r\n/**\r\n * `<KnockCodes>` fixed to the \"inline\" shell — sits naturally inside an\r\n * existing layout (a sidebar widget, a section of a longer page) instead of\r\n * taking over the full viewport like the page-variant default.\r\n */\r\nexport function EmbeddedGate(props: EmbeddedGateProps) {\r\n  return <KnockCodes {...props} variant=\"inline\" />;\r\n}\r\n",
      "type": "registry:file",
      "target": "components/knock-codes/react/EmbeddedGate.tsx"
    }
  ],
  "type": "registry:block"
}