# CLAUDE.md

This project uses Knock Codes. Before touching any PIN, hash, or env file in this
project, read and follow `AGENTS.md` in full — it is the canonical install/config
contract and takes precedence over general habits (e.g. Claude Code must not use
its usual patterns for secrets if they conflict with AGENTS.md's hash-only rule).
