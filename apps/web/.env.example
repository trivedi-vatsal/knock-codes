# Deployed site origin used to build shadcn install commands (npx shadcn add <url>)
# and page metadataBase. Production builds already default to https://knock.codes —
# only set this if you're deploying to a different domain (a fork, a preview URL).
NEXT_PUBLIC_SITE_URL=https://knock.codes
